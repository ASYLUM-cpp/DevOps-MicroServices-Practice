# Example environment variables for all services
# Copy this to .env in each service directory and update values

# ===========================================
# Gateway Service (.env)
# ===========================================
PORT=5000
AUTH_SERVICE_URL=http://auth-service:5001
TASK_SERVICE_URL=http://task-service:5002
NODE_ENV=development

# ===========================================
# Auth Service (.env)
# ===========================================
# PORT=5001
# JWT_SECRET=change-this-to-a-strong-secret-in-production-min-32-chars
# JWT_EXPIRY=24h
# NODE_ENV=development

# ===========================================
# Task Service (.env)
# ===========================================
# PORT=5002
# JWT_SECRET=change-this-to-a-strong-secret-in-production-min-32-chars
# NODE_ENV=development

# ===========================================
# Security Notes
# ===========================================
# 1. NEVER commit real .env files to git
# 2. Use strong JWT_SECRET (min 32 characters, random)
# 3. Change all secrets before deploying to production
# 4. Use different secrets for different environments
# 5. Consider using a secret management tool (AWS Secrets Manager, HashiCorp Vault, etc.)

# ===========================================
# Production Recommendations
# ===========================================
# - Set NODE_ENV=production
# - Use a real database (MongoDB, PostgreSQL)
# - Enable HTTPS
# - Implement rate limiting
# - Add monitoring and logging
# - Use environment-specific secrets
